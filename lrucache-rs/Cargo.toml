[package]
name = "lrucache_rs"
version = "1.4.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
name = "lrucache_rs"

[dependencies]
ordered_hash_map = "0.5.0"
pyo3 = { version = "0.27.2", features = ["extension-module"] }

[profile.release]
overflow-checks = true
lto = true
codegen-units = 1
