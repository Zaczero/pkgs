# Third-Party Licenses

This package bundles the third-party Rust libraries listed below — statically
linked into the compiled extension, and vendored as build inputs in source
distributions. The copyright notice and full license text for each are
reproduced below. The package's own code is licensed separately, as stated in
its package metadata.

Generated from the dependency graph by [`cargo about`][cargo-about]; do not edit
by hand. Regenerate with `.github/scripts/gen_licenses.py`.

[cargo-about]: https://github.com/EmbarkStudios/cargo-about

## Summary

{{#each overview}}
- {{{name}}} — {{count}}
{{/each}}
{{#each licenses}}

---

## {{{name}}}

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} <{{crate.repository}}>{{/if}}
{{/each}}

```
{{{text}}}
```
{{/each}}
