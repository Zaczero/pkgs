# Third-Party Licenses

This package's compiled extension statically links the third-party Rust libraries
listed below. The source of those libraries is not redistributed, but a
statically linked binary *is* a binary redistribution, so each library's
copyright notice and license terms are reproduced here in full — as the MIT, BSD,
ISC, and Apache-2.0 licenses require. The package's own code is provided under the
license declared in its package metadata.

This file is generated from the dependency graph by [`cargo about`][cargo-about];
do not edit it by hand. Regenerate it with
`.github/scripts/gen_third_party_licenses.py`.

[cargo-about]: https://github.com/EmbarkStudios/cargo-about

## Summary

{{#each overview}}
- {{{name}}} — {{count}}
{{/each}}
{{#each licenses}}

---

## {{{name}}}

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} <{{crate.repository}}>{{/if}}
{{/each}}

```
{{{text}}}
```
{{/each}}
