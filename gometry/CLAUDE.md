# Gometry agent entrypoint

@AGENTS.md

The binding project rules and durable architecture live in `AGENTS.md`. The
current whole-plan execution state lives in `V1_RELEASE_COHERENCE_TASKS.md`; read it
before resuming implementation and keep it current as work is verified.

Gometry is the Rust-first package rooted at this directory.
